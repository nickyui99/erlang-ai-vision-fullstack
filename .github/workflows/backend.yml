name: backend

on:
  pull_request:
    paths:
      - "backend/**"
      - ".github/workflows/backend.yml"
  push:
    branches: [main]
    paths:
      - "backend/**"
      - ".github/workflows/backend.yml"

concurrency:
  group: backend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install dependencies
        run: pip install -r backend/requirements-dev.txt

      - name: Run tests
        env:
          APP_ENV: test
          PYTHONPATH: backend
        run: pytest backend/tests

      - name: Build image (verify Dockerfile)
        run: docker build -f backend/Dockerfile -t erlang-ai-vision-backend:ci .

  # Deploy: stubbed until a target (VM/SAE) is chosen. When ready, this job will
  # log in to ACR, push the image tagged by SHA, then ship it to the target
  # (mirrors scripts/deploy.ps1). Gated on `build` passing + main only.
  #
  # deploy:
  #   needs: build
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   environment: production
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Log in to ACR
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ secrets.ACR_REGISTRY }}
  #         username: ${{ secrets.ACR_USERNAME }}
  #         password: ${{ secrets.ACR_PASSWORD }}
  #     - name: Build and push
  #       uses: docker/build-push-action@v6
  #       with:
  #         context: .
  #         file: backend/Dockerfile
  #         push: true
  #         tags: ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/backend:${{ github.sha }}
  #     - name: Deploy to target
  #       run: echo "wire VM ssh (BACKEND_IMAGE=... docker compose up -d) or SAE deploy here"
